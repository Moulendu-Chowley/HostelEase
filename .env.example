# Supabase project settings
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=

# DeepFace recognition service (Python)
DEEPFACE_API_URL=http://localhost:8000
NEXT_PUBLIC_DEEPFACE_SERVICE_URL=http://localhost:8000

# Database setup is done via SQL scripts:
# 1) Run supabase/schema.sql  (includes photo_url column + storage bucket)
# 2) Run supabase/seed.sql
# 3) Start Backend service: cd backend && pip install -r requirements.txt && python main.py
